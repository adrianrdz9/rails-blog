.container
    %h1.text-center Categorias
    .list-group
        -@topics.each do |topic|
            =link_to topic_path(topic.id), type:"button", class:"list-group-item" do
                = topic.topic
                %span.badge= Post.where("subject = ?", topic.topic).count
